import { createTheme } from "@mui/material";

export const theeme = createTheme({
    palette: {
        primary: {
            main: "#E5AB24",
            contrastText: "#fff",
        },
        secondary: {
            main: "#3D348B",
            contrastText: "#fff",
        },
    },
});
