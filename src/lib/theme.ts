import { createTheme } from "@mui/material";

export const theeme = createTheme({
    palette: {
        primary: {
            main: "#E5AB24",
        },
        secondary: {
            main: "#3D348B",
            contrastText: "#000",
        },
    },
});
