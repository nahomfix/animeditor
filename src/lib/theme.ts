import { createTheme } from "@mui/material";

export const theeme = createTheme({
    palette: {
        primary: {
            main: "#F46036",
            contrastText: "#fff",
        },
        secondary: {
            main: "#3D348B",
            contrastText: "#fff",
        },
    },
});
